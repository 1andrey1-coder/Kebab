<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Kebab.XAML.ProductsPage">
 <ContentPage.Content>
    <StackLayout>


        <CollectionView  SelectedItem="{Binding SelectedItem}"
                         ItemsSource="{Binding List}" SelectionMode="Multiple">
                <CollectionView.ItemTemplate >
                <DataTemplate>
                    <StackLayout>
                            <StackLayout Orientation="Horizontal">
                                <StackLayout WidthRequest="150">
                                    
                                    <Label Text="Название: " FontFamily="Times New Roman"/>
                                    <Label Text="{Binding Name}" Margin="0,-5,0,0"/>
                                    <Image Source="{Binding Image}"/>
                                    <StackLayout WidthRequest="150">
                                        <Label Text="Категория: " />
                                        <Label Text="{Binding Category}" /> 
                                    </StackLayout>
                                </StackLayout>
                              
                                <StackLayout>
                                    <Label Text="Описание:"/>
                                    <Label Text="{Binding Title}" Margin="0,-5,0,0"/>
                                </StackLayout>
                            </StackLayout>

                            
                            <StackLayout Orientation="Horizontal" >

                                <Button HeightRequest="50" WidthRequest="150" Text="Редактировать" 
                                    Clicked="RedactorClick"/>
                            <Button 
                                Text="Удалить" HeightRequest="50" WidthRequest="100"  Clicked="DeleteClick"/>
                            </StackLayout>
                    </StackLayout>
                </DataTemplate>
           </CollectionView.ItemTemplate>
        </CollectionView>


        </StackLayout>
   </ContentPage.Content>
</ContentPage>